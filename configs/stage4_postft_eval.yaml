name: stage4_postft_eval
checkpoints:
  - runs/stage3_finetune_causal/final
  - runs/stage3_finetune_prefill_bidir/final
tasks:
  - hellaswag
  - piqa
  - arc_easy
  - arc_challenge
  - winogrande
split: validation
limit_per_task: 500
dtype: bfloat16
attn_implementation: sdpa
prefill_bidirectional: true
length_normalize: true
success_criteria:
  - "Recovered model reaches at least 80% of baseline macro accuracy from stage1."
  - "Recovered model improves over stage2 ablation by >= 5 macro points."
